# Docs Zone Override (`docs/`)

This directory is stable reference context.

- Default behavior: read-first, minimal edits.
- Only edit docs when explicitly requested by the user.
- When editing, keep terminology and structure backward compatible.
- Prefer additive changes over rewrites.
- Call out any statements that are assumptions vs verified facts.
